#include <stdlib.h>

#include "turn_gps_fishtail.h"

void TURN_gps_fishtail(const pointtype dmp_i,
		       const pointtype dmp_ip1,
		       const pointtype dmp_ip2,
		       const double turn_radius,
		       const double speed,
		       const double acc,
		       const double alt_speed)
{
  exit(-1); /* Not implemented yet! */
}
