#ifndef STOP_TRACTOR_HDR
#define STOP_TRACTOR_HDR

void stop_tractor(void);
/* This function will send a command sequence to the Hako in order
 * to make it stop. */
/* Intended to be called by "smart" navigatin routines when they
 * are passed arguments they cannot handle. */

#endif /* STOP_TRACTOR_HDR */
