#ifndef TURN_WIDE_HDR
#define TURN_WIDE_HDR

#include "functions.h"
/* The above in order to get 'point' definition. */

/* Prototype for function making wide turns ... (i.e. circling). */
int TURN_wide(const pointtype dmp_i, const pointtype dmp_ip1, const pointtype dmp_ip2);

#endif /* TURN_WIDE_HDR */
